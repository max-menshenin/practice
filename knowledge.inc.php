
        <!-- integer — целые числа;
        double — дробные числа;
        string — строки;
        object — объекты;
        array — массивы;
        boolean — логический тип;
        resource — ресурсы;
        NULL. -->


<?php
$a = 'Мои знания на';
$b = 100;
$c = '%'; 
?>

<?php
  $price = 15;

  if($price >= 15 && $price <= 20)
      $d = 'Это число находится между 15 и 20';
?>